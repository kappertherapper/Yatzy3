doctype html
html
  head
    title Waiting Lobby
    link(rel="stylesheet", href="/style/waitinglobby.css")
  body
    h1 Waiting Lobby

    h2 Players in Game
    table
      tr
        th Name
        each user in playersInGame
          tr
            td= user.name

    h2 Players in Queue
    ul#queueList
      if playersInQueue && playersInQueue.length > 0
        each player in playersInQueue
          li= player
      else
        li No players in queue

    script(src="/path/to/timer.js")